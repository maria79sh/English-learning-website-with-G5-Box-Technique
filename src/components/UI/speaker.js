import Speaker from "../../assets/icons/speaker.png"

const SpeakerIcon = () => {
    return (
        <img src={Speaker} alt="user profile icon"  style={{width: "80px"}}/>
    );
};

export default SpeakerIcon;
